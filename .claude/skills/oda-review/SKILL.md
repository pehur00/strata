---
name: oda-review
description: Full ODA (TM Forum Open Digital Architecture) architecture review. Scores maturity across 6 dimensions, checks ODA component coverage, identifies missing documentation, and produces a prioritised to-do list with actionable decisions. Reads from a configurable input source; writes output to a configurable output folder.
allowed-tools: Read, Glob, Grep, Bash
argument-hint: [scope? e.g. score | improve | oda | docs | decide | all]
---

This skill has moved to a standalone repository.

Read the canonical skill file and follow its instructions exactly:

```
Read /Users/jasperpoc/Documents/projects/oda-skill/SKILL.md
```

Then proceed with those instructions using:
- `$ARGUMENTS` as the scope/path argument
- Current working directory as the fallback input root
